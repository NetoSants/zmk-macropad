menuconfig ENCODER_CUSTOM
	bool "Custom EC11 Encoder Driver"
	default y
	help
	  Custom quadrature encoder driver for EC11 that directly sends
	  HID consumer reports, bypassing ZMK sensor subsystem.
